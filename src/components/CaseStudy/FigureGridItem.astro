---
import Image from "../Image.jsx";

const { src, alt, caption = alt, col = "md-6" } = Astro.props;
---

<Image
  client:visible
  src={src}
  alt={alt}
  caption={caption}
  figureClassName={`col-${col}`}
/>
