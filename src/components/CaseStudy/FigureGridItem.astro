---
import Figure from "./Figure.astro";

const { src, alt, caption = alt, raw = false, col = "md-6" } = Astro.props;
---

<Figure
  src={src}
  alt={alt}
  caption={caption}
  raw={raw}
  figureClass={`col-${col}`}
/>
